# Terraform foundation

This directory was generated for **{{ cloud }}**.

It intentionally configures the provider only. Add application resources as explicit infrastructure modules instead of hiding infrastructure assumptions inside the project scaffold.

```bash
terraform init
terraform fmt -check
terraform validate
terraform plan
```

Do not commit Terraform state or credentials.
