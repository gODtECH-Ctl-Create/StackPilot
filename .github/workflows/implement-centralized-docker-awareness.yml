name: Implement centralized Docker awareness

on:
  push:
    branches:
      - fix/centralized-docker-awareness

permissions:
  contents: write

jobs:
  implement:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v6
        with:
          ref: fix/centralized-docker-awareness
          fetch-depth: 0

      - name: Patch centralized Docker ownership
        shell: python
        run: |
          from pathlib import Path

          inspect = Path('src/inspect.rs')
          text = inspect.read_text()

          old = '''    let docker = detect_docker(&root, &files);\n    let mut ci = detect_ci(&repository_root, &repository_files);\n'''
          new = '''    let mut docker = detect_docker(&root, &files);\n    if nested_target && !docker.detected {\n        if let Some(inherited) =\n            detect_inherited_docker(&repository_root, &root, &repository_files)\n        {\n            docker = inherited;\n        }\n    }\n    let mut ci = detect_ci(&repository_root, &repository_files);\n'''
          assert old in text
          text = text.replace(old, new, 1)

          old = '''    findings.extend(security::inspect_scoped(&repository_root, &root)?);\n'''
          new = '''    findings.extend(security::inspect_scoped(\n        &repository_root,\n        &root,\n        docker.dockerfile,\n    )?);\n'''
          assert old in text
          text = text.replace(old, new, 1)

          old = '''#[derive(Debug)]\nstruct Detection {\n    detected: bool,\n    detail: String,\n}\n\n#[derive(Debug)]\nstruct EnvironmentDetection {\n'''
          new = '''#[derive(Debug)]\nstruct Detection {\n    detected: bool,\n    detail: String,\n}\n\n#[derive(Debug)]\nstruct DockerDetection {\n    detected: bool,\n    dockerfile: bool,\n    detail: String,\n}\n\n#[derive(Debug)]\nstruct EnvironmentDetection {\n'''
          assert old in text
          text = text.replace(old, new, 1)

          start = text.index('fn detect_docker(root: &Path, files: &[PathBuf]) -> Detection {')
          end = text.index('\nfn detect_ci(root: &Path, files: &[PathBuf]) -> Detection {', start)
          replacement = r'''fn detect_docker(root: &Path, files: &[PathBuf]) -> DockerDetection {
    let mut dockerfiles = Vec::new();
    let mut compose_files = Vec::new();

    for path in files {
        let Some(name) = path.file_name().and_then(|name| name.to_str()) else {
            continue;
        };
        let lower = name.to_ascii_lowercase();

        if is_dockerfile_name(&lower) {
            dockerfiles.push(relative_path(root, path));
        }
        if is_compose_name(&lower) {
            compose_files.push(relative_path(root, path));
        }
    }

    DockerDetection {
        detected: !dockerfiles.is_empty() || !compose_files.is_empty(),
        dockerfile: !dockerfiles.is_empty(),
        detail: match (dockerfiles.first(), compose_files.first()) {
            (Some(dockerfile), Some(compose)) => {
                format!("Dockerfile and Compose detected ({dockerfile}, {compose})")
            }
            (Some(dockerfile), None) => format!("Dockerfile detected ({dockerfile})"),
            (None, Some(compose)) => format!("Compose detected ({compose})"),
            (None, None) => "No Dockerfile or Compose configuration detected".to_string(),
        },
    }
}

fn detect_inherited_docker(
    repository_root: &Path,
    target_root: &Path,
    repository_files: &[PathBuf],
) -> Option<DockerDetection> {
    let target_relative = relative_path(repository_root, target_root);
    let target_relative = target_relative.trim_matches('/');
    let service_name = target_root.file_name()?.to_str()?;
    if target_relative.is_empty() || service_name.is_empty() {
        return None;
    }

    let dockerfiles = repository_files
        .iter()
        .filter(|path| !path.starts_with(target_root))
        .filter(|path| {
            path.file_name()
                .and_then(|name| name.to_str())
                .map(str::to_ascii_lowercase)
                .is_some_and(|name| is_dockerfile_name(&name))
        })
        .collect::<Vec<_>>();
    let compose_files = repository_files
        .iter()
        .filter(|path| {
            path.file_name()
                .and_then(|name| name.to_str())
                .map(str::to_ascii_lowercase)
                .is_some_and(|name| is_compose_name(&name))
        })
        .collect::<Vec<_>>();

    let mut matches = Vec::new();
    for dockerfile in dockerfiles {
        let Some(dockerfile_text) = read_small_text(dockerfile) else {
            continue;
        };
        if !dockerfile_targets_path(&dockerfile_text, target_relative) {
            continue;
        }

        let dockerfile_relative = relative_path(repository_root, dockerfile);
        for compose in &compose_files {
            let Some(compose_text) = read_small_text(compose) else {
                continue;
            };
            if compose_service_references_dockerfile(
                &compose_text,
                service_name,
                &dockerfile_relative,
            ) {
                matches.push((
                    dockerfile_relative.clone(),
                    relative_path(repository_root, compose),
                ));
            }
        }
    }

    matches.sort();
    matches.dedup();
    if matches.len() != 1 {
        return None;
    }

    let (dockerfile, compose) = matches.remove(0);
    Some(DockerDetection {
        detected: true,
        dockerfile: true,
        detail: format!(
            "Inherited from repository root: centralized Dockerfile and Compose detected ({dockerfile}, {compose})"
        ),
    })
}

fn is_dockerfile_name(lower_name: &str) -> bool {
    lower_name == "dockerfile"
        || lower_name.starts_with("dockerfile.")
        || lower_name.ends_with(".dockerfile")
}

fn is_compose_name(lower_name: &str) -> bool {
    matches!(
        lower_name,
        "compose.yml" | "compose.yaml" | "docker-compose.yml" | "docker-compose.yaml"
    )
}

fn dockerfile_targets_path(content: &str, target_relative: &str) -> bool {
    let target = target_relative.replace('\\', "/").to_ascii_lowercase();
    content.lines().any(|line| {
        let trimmed = line.trim_start();
        let lower = trimmed.to_ascii_lowercase().replace('\\', "/");
        (lower.starts_with("copy ") || lower.starts_with("add ")) && lower.contains(&target)
    })
}

fn compose_service_references_dockerfile(
    content: &str,
    service_name: &str,
    dockerfile_relative: &str,
) -> bool {
    let expected_service = format!("{service_name}:");
    let expected_dockerfile = dockerfile_relative
        .replace('\\', "/")
        .trim_start_matches("./")
        .to_ascii_lowercase();
    let mut services_indent = None;
    let mut service_indent = None;

    for line in content.lines() {
        let trimmed = line.trim();
        if trimmed.is_empty() || trimmed.starts_with('#') {
            continue;
        }
        let indent = line.chars().take_while(|character| character.is_whitespace()).count();

        if services_indent.is_none() {
            if trimmed == "services:" {
                services_indent = Some(indent);
            }
            continue;
        }

        let services_indent_value = services_indent.expect("services indent");
        if service_indent.is_none() {
            if indent <= services_indent_value && trimmed != "services:" {
                return false;
            }
            if trimmed == expected_service && indent > services_indent_value {
                service_indent = Some(indent);
            }
            continue;
        }

        let service_indent_value = service_indent.expect("service indent");
        if indent <= service_indent_value {
            return false;
        }
        let Some(value) = trimmed.strip_prefix("dockerfile:") else {
            continue;
        };
        let value = value
            .trim()
            .trim_matches(|character| matches!(character, '\'' | '"'))
            .replace('\\', "/")
            .trim_start_matches("./")
            .to_ascii_lowercase();
        if value == expected_dockerfile {
            return true;
        }
    }

    false
}
'''
          text = text[:start] + replacement + text[end:]

          marker = '''        assert_eq!(report.frameworks, vec!["NestJS".to_string()]);\n        let ci = report.finding("CI/CD").expect("CI finding");\n'''
          addition = '''        assert_eq!(report.frameworks, vec!["NestJS".to_string()]);\n        let docker = report.finding("Docker").expect("Docker finding");\n        assert_eq!(docker.status, FindingStatus::Passed);\n        assert!(docker.detail.contains("Inherited from repository root"));\n        assert!(docker.detail.contains("infra/docker/api.Dockerfile"));\n        let ci = report.finding("CI/CD").expect("CI finding");\n'''
          assert marker in text
          text = text.replace(marker, addition, 1)

          setup_marker = '''        fs::write(\n            repo.path().join("pnpm-lock.yaml"),\n            "lockfileVersion: '9.0'\\n",\n        )\n        .expect("lockfile");\n\n        let service = repo.path().join("services/api");\n'''
          setup_addition = '''        fs::write(\n            repo.path().join("pnpm-lock.yaml"),\n            "lockfileVersion: '9.0'\\n",\n        )\n        .expect("lockfile");\n        fs::create_dir_all(repo.path().join("infra/docker")).expect("docker directory");\n        fs::write(\n            repo.path().join("infra/docker/api.Dockerfile"),\n            "FROM node:22-alpine\\nCOPY services/api/package.json services/api/package.json\\nCOPY services/api services/api\\n",\n        )\n        .expect("centralized API Dockerfile");\n        fs::write(\n            repo.path().join("infra/docker/docker-compose.yml"),\n            "services:\\n  api:\\n    build:\\n      context: ../..\\n      dockerfile: infra/docker/api.Dockerfile\\n",\n        )\n        .expect("centralized Compose");\n\n        let service = repo.path().join("services/api");\n'''
          assert setup_marker in text
          text = text.replace(setup_marker, setup_addition, 1)

          insert_before = '\n}\n'
          test = r'''

    #[test]
    fn nested_service_does_not_inherit_unrelated_centralized_dockerfile() {
        let repo = tempdir().expect("repository");
        fs::create_dir(repo.path().join(".git")).expect("git marker");
        fs::create_dir_all(repo.path().join("infra/docker")).expect("docker directory");
        fs::write(
            repo.path().join("infra/docker/worker.Dockerfile"),
            "FROM node:22-alpine\nCOPY services/worker services/worker\n",
        )
        .expect("worker Dockerfile");
        fs::write(
            repo.path().join("infra/docker/docker-compose.yml"),
            "services:\n  worker:\n    build:\n      context: ../..\n      dockerfile: infra/docker/worker.Dockerfile\n",
        )
        .expect("compose");

        let service = repo.path().join("services/api");
        fs::create_dir_all(service.join("src")).expect("service source");
        fs::write(
            service.join("package.json"),
            r#"{"dependencies":{"@nestjs/core":"latest"},"devDependencies":{"typescript":"latest"}}"#,
        )
        .expect("package.json");
        fs::write(service.join("tsconfig.json"), "{}").expect("tsconfig");

        let report = inspect_repository(&service).expect("inspection");
        assert_eq!(
            report.finding("Docker").expect("Docker finding").status,
            FindingStatus::Missing
        );
    }
'''
          pos = text.rfind(insert_before)
          assert pos != -1
          text = text[:pos] + test + text[pos:]
          inspect.write_text(text)

          security = Path('src/security.rs')
          text = security.read_text()
          text = text.replace(
              '    inspect_scoped(root, root)\n',
              '    inspect_scoped(root, root, has_direct_dockerfile(root))\n',
              1,
          )
          text = text.replace(
              'pub fn inspect_scoped(repository_root: &Path, target_root: &Path) -> Result<Vec<Finding>> {',
              'pub fn inspect_scoped(\n    repository_root: &Path,\n    _target_root: &Path,\n    target_has_dockerfile: bool,\n) -> Result<Vec<Finding>> {',
              1,
          )
          text = text.replace(
              '} else if target_root.join("Dockerfile").is_file() {',
              '} else if target_has_dockerfile {',
              1,
          )
          marker = '\nfn contains_any(content: &str, markers: &[&str]) -> bool {\n'
          helper = r'''
fn has_direct_dockerfile(root: &Path) -> bool {
    let Ok(entries) = fs::read_dir(root) else {
        return false;
    };
    entries.flatten().any(|entry| {
        let path = entry.path();
        if !path.is_file() {
            return false;
        }
        path.file_name()
            .and_then(|name| name.to_str())
            .map(str::to_ascii_lowercase)
            .is_some_and(|name| {
                name == "dockerfile"
                    || name.starts_with("dockerfile.")
                    || name.ends_with(".dockerfile")
            })
    })
}

'''
          assert marker in text
          text = text.replace(marker, '\n' + helper + 'fn contains_any(content: &str, markers: &[&str]) -> bool {\n', 1)
          security.write_text(text)

          fix = Path('src/fix.rs')
          text = fix.read_text()
          pos = text.rfind('\n}\n')
          assert pos != -1
          test = r'''

    #[test]
    fn nested_service_with_centralized_docker_does_not_plan_duplicate_container_files() {
        let repo = tempdir().expect("repository");
        fs::create_dir(repo.path().join(".git")).expect("git marker");
        fs::create_dir_all(repo.path().join("infra/docker")).expect("docker directory");
        fs::write(
            repo.path().join("infra/docker/api.Dockerfile"),
            "FROM node:22-alpine\nCOPY services/api/package.json services/api/package.json\nCOPY services/api services/api\n",
        )
        .expect("centralized API Dockerfile");
        fs::write(
            repo.path().join("infra/docker/docker-compose.yml"),
            "services:\n  api:\n    build:\n      context: ../..\n      dockerfile: infra/docker/api.Dockerfile\n",
        )
        .expect("compose");

        let service = repo.path().join("services/api");
        fs::create_dir_all(service.join("src")).expect("service source");
        fs::write(
            service.join("package.json"),
            r#"{"dependencies":{"@nestjs/core":"latest"},"devDependencies":{"typescript":"latest"}}"#,
        )
        .expect("package.json");
        fs::write(service.join("tsconfig.json"), "{}").expect("tsconfig");
        fs::write(
            service.join("src/main.ts"),
            "app.get('/health', () => ({ status: 'ok' }));",
        )
        .expect("source");

        let plan = plan_repository(&service, Path::new("recipes"), None).expect("fix plan");
        for path in ["Dockerfile", "compose.yaml", ".dockerignore"] {
            assert!(
                !plan.changes.iter().any(|change| change.path == Path::new(path)),
                "centralized container ownership must suppress duplicate {path} remediation"
            );
        }
        assert!(
            plan.changes
                .iter()
                .any(|change| change.path == Path::new(".stackpilot.toml"))
        );
    }
'''
          text = text[:pos] + test + text[pos:]
          fix.write_text(text)

          smoke = Path('.github/workflows/monorepo-awareness-smoke.yml')
          text = smoke.read_text()
          text = text.replace(
              '          mkdir -p "$ROOT/.git" "$ROOT/.github/workflows" "$SERVICE/src"\n',
              '          mkdir -p "$ROOT/.git" "$ROOT/.github/workflows" "$ROOT/infra/docker" "$SERVICE/src"\n',
              1,
          )
          needle = '''          printf "lockfileVersion: '9.0'\\n" > "$ROOT/pnpm-lock.yaml"\n\n          cat > "$SERVICE/package.json" <<'JSON'\n'''
          addition = '''          printf "lockfileVersion: '9.0'\\n" > "$ROOT/pnpm-lock.yaml"\n          cat > "$ROOT/infra/docker/api.Dockerfile" <<'DOCKER'\n          FROM node:22-alpine\n          COPY services/api/package.json services/api/package.json\n          COPY services/api services/api\n          DOCKER\n          cat > "$ROOT/infra/docker/docker-compose.yml" <<'YAML'\n          services:\n            api:\n              build:\n                context: ../..\n                dockerfile: infra/docker/api.Dockerfile\n          YAML\n\n          cat > "$SERVICE/package.json" <<'JSON'\n'''
          assert needle in text
          text = text.replace(needle, addition, 1)
          text = text.replace(
              "          grep -F 'StackPilot Readiness: 70/100' /tmp/nested-inspect.txt\n",
              "          grep -F 'StackPilot Readiness: 85/100' /tmp/nested-inspect.txt\n          grep -F 'Inherited from repository root: centralized Dockerfile and Compose detected (infra/docker/api.Dockerfile, infra/docker/docker-compose.yml)' /tmp/nested-inspect.txt\n",
              1,
          )
          text = text.replace(
              "          grep -F '+ create Dockerfile' /tmp/nested-fix.txt\n          grep -F '+ create .stackpilot.toml' /tmp/nested-fix.txt\n",
              "          grep -F '+ create .stackpilot.toml' /tmp/nested-fix.txt\n          if grep -Fq '+ create Dockerfile' /tmp/nested-fix.txt || grep -Fq '+ create compose.yaml' /tmp/nested-fix.txt || grep -Fq '+ create .dockerignore' /tmp/nested-fix.txt; then\n            echo 'centralized Docker ownership must suppress duplicate container remediation'\n            exit 1\n          fi\n",
              1,
          )
          smoke.write_text(text)

      - name: Format
        run: cargo fmt --all

      - name: Test
        run: cargo test --locked

      - name: Clippy
        run: cargo clippy --locked --all-targets -- -D warnings

      - name: Commit implementation
        shell: bash
        run: |
          set -euo pipefail
          git add src/inspect.rs src/security.rs src/fix.rs .github/workflows/monorepo-awareness-smoke.yml
          git diff --cached --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "fix: detect centralized Docker ownership for nested services"
          git push origin HEAD:fix/centralized-docker-awareness
