name: Dispatch v0.2.6 release helper

on:
  push:
    branches:
      - chore/dispatch-v026-release
    paths:
      - .github/workflows/dispatch-v026-release.yml

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    if: github.actor == 'gODtECH-Ctl-Create'
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch canonical Release workflow
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          gh workflow run release.yml \
            --repo "$GITHUB_REPOSITORY" \
            --ref main \
            -f tag=v0.2.6
